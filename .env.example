# Gemini API Key (必須)
GOOGLE_API_KEY=your_gemini_api_key_here

# 是否使用 GCS (設為 true 使用 GCP bucket，false 使用本地儲存)
# 預設: false (本地儲存)
USE_GCS=false

# GCS Bucket 名稱 (只在 USE_GCS=true 時需要)
GCS_BUCKET_NAME=team-bubu

# 服務基礎 URL
BASE_URL=http://localhost:8000
